<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/mainScrollView"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fillViewport="true"
    android:padding="16dp"
    android:background="@color/dark_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/card_background">

        <!-- Добавленный Toolbar -->
        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="?attr/actionBarSize"
            android:background="@color/primary_color"
            android:elevation="4dp"
            android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
            app:title="Настройки цветов"
            app:titleTextColor="@color/text_primary"/>

        <!-- Переключатель темы -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingVertical="16dp"
            android:focusable="true"
            android:clickable="true"
            android:contentDescription="Переключатель темной темы"
            android:background="@color/card_background">

            <TextView
                android:id="@+id/themeLabel"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Темная тема"
                android:textSize="16sp"
                android:textColor="@color/text_primary"
                android:labelFor="@+id/themeSwitch"/>

            <androidx.appcompat.widget.SwitchCompat
                android:id="@+id/themeSwitch"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:contentDescription="Темная тема"
                android:importantForAccessibility="yes"/>
        </LinearLayout>

        <!-- Дневные цвета -->
        <TextView
            android:id="@+id/dayColorsLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Дневные цвета"
            android:textStyle="bold"
            android:textSize="18sp"
            android:paddingTop="16dp"
            android:textColor="@color/text_primary"
            android:background="@color/card_background"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/dayColorsRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:background="@color/dark_background"/>

        <!-- Ночные цвета -->
        <TextView
            android:id="@+id/nightColorsLabel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Ночные цвета"
            android:textStyle="bold"
            android:textSize="18sp"
            android:paddingTop="16dp"
            android:textColor="@color/text_primary"
            android:visibility="gone"
            android:background="@color/card_background"/>

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/nightColorsRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:visibility="gone"
            android:background="@color/dark_background"/>

        <!-- Кнопки -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center"
            android:paddingTop="24dp"
            android:background="@color/card_background">

            <Button
                android:id="@+id/saveButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginEnd="8dp"
                android:text="Сохранить"
                android:textColor="@color/text_button"
                android:contentDescription="Сохранить настройки цветов"
                android:importantForAccessibility="yes"
                android:backgroundTint="@color/accent_color"/>

            <Button
                android:id="@+id/resetButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:text="Сброс"
                android:textColor="@color/text_button"
                android:contentDescription="Сбросить настройки цветов"
                android:importantForAccessibility="yes"
                android:backgroundTint="@color/grey_1"/>
        </LinearLayout>
    </LinearLayout>
</ScrollView>